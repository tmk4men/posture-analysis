# 所見文（diagnosis）生成ルールブック

POSTURA Studio のレポート1ページ目に載る **所見文（患者向けコメント）** を、
計測値・申告部位・週頻度から **決定的に（AIを使わず）** 組み立てるための完全ルール定義。

> **なぜルール化したか**：以前は Gemini / OpenAI / Anthropic に計測値を送って文章を
> 生成させていた。これを廃止し、同じ入力からは必ず同じ文章が出る本ルールに置き換えた。
> 外部送信ゼロ・APIキー不要・オフライン動作・出力が毎回ブレない、という利点がある。

- 実装：`src/pose/diagnosis.js`（`buildDiagnosis()`）
- しきい値の共有元：`src/pose/thresholds.js`（`recommend.js` の筋肉・種目選定と同一値）
- 呼び出し口：`src/pose/recommend.js` の `deriveRecommendations()` が `diagnosis` を含めて返す

**最終更新**：2026-07-13

---

## 0. 文章の全体方針

| 項目 | ルール |
|---|---|
| 長さ | 3〜4文、目安 120〜180字（所見が無い理想姿勢のみ 2〜3文・短めを許容） |
| 語り口 | 生活者向けアプリの口調。ですます調。前向きで平易 |
| 数値 | 本文に **出さない**（"+12.3%" 等は禁止）。度合い語に変換 |
| 分類ラベル | 「猫背タイプ」「反り腰タイプ」等は **使わない**。見えた特徴をそのまま描写 |
| 医療的表現 | 「診断」「所見」「症状」「疾患」等の医療語は **使わない**（アプリとしてのトーン維持） |
| 申告部位 | 入力があれば **必ず1つ**やわらかく触れる |
| 見通し | 最終文で **週N回** を反映した前向きな一文を必ず添える |

---

## 1. 入力

`buildDiagnosis(metricsByView, painAreas, weeklyFrequency)`

- **metricsByView**：`{ front: [...], right: [...] }`。各要素は `{ key, value, ... }`。
  側面は `right` を優先、無ければ `left`。
- **painAreas**：申告部位IDの配列（例 `["shoulder","lowback"]`）。空配列可。
- **weeklyFrequency**：1〜5の整数（範囲外は 2 に丸め、5超は 5 に丸め）。

計測キーと意味・符号：

| key | ビュー | 意味 | 符号の意味 |
|---|---|---|---|
| `forward_head` | 側面 | 頭部前方位（体幹高比％） | ＋=頭が前 / −=頭が後ろ |
| `shoulder_forward` | 側面 | 肩の前方変位（体幹高比％） | ＋=巻き肩 / −=肩が後ろ |
| `trunk_lean` | 側面 | 体幹の前後傾（度） | ＋=前傾 / −=後傾 |
| `knee_angle` | 側面 | 膝角度 hip-knee-ankle（度） | ≥178=過伸展 / <165=屈曲位 |
| `shoulder_tilt` | 正面 | 肩の傾き（度） | ＋=右肩が下がり / −=左肩が下がり |
| `pelvic_tilt` | 正面 | 骨盤の傾き（度） | ＋=右骨盤が下がり / −=左骨盤が下がり |
| `head_tilt` | 正面 | 頭部の傾き（度） | ＋=右へ傾斜 / −=左へ傾斜 |
| `lateral_shift` | 正面 | 上半身の左右シフト（肩幅比％） | ＋=右へシフト / −=左へシフト |

---

## 2. しきい値定義（`src/pose/thresholds.js`）

所見を拾い始める基準値 `WARN`（＝臨床的に注意したいライン）。
**逸脱の大きさ ratio = |計測値| ÷ WARN** で正規化する（1.0 = しきい値ちょうど）。

| key | WARN | 単位 |
|---|---|---|
| forward_head | 10 | 体幹高比％ |
| shoulder_forward | 8 | 体幹高比％ |
| trunk_lean | 5 | 度 |
| shoulder_tilt | 2 | 度 |
| pelvic_tilt | 2 | 度 |
| head_tilt | 3 | 度 |
| lateral_shift | 5 | 肩幅比％ |
| knee（過伸展） | 178° を超えた分を `knee_hyper=2` で正規化 | 度 |
| knee（屈曲位） | 165° を下回った分を `knee_flex=5` で正規化 | 度 |

**採用条件**：`ratio ≥ ENTRY_RATIO(=0.5)` の所見だけを文章化する
（＝しきい値の半分未満は「ほぼ問題なし」として触れない）。
膝は 178°以上／165°未満に入った時点で採用（`ratio = 逸脱/係数 + 1`、最低1.0）。

> **チューニング要点**：この表の値を変えると、所見文と筋肉・種目選定の両方が
> 同時に反応する（単一の真実源）。特に膝は「178°以上＝過伸展」なので、
> まっすぐ立った脚（MediaPipe で 178〜180°になりがち）でも過伸展と出やすい。
> 過検出が気になる場合は `thresholds.js` の判定 `>= 178` を上げる（例 182）。

---

## 3. 度合い語（severity → 副詞）

採用した所見の `ratio` を、本文中の度合い語に変換する。

| ratio | 度合い語 | 意味 |
|---|---|---|
| 0.5 ≤ ratio < 1.0 | **わずかに** | しきい値未満の軽い傾向 |
| 1.0 ≤ ratio < 2.0 | **やや** | しきい値〜2倍 |
| 2.0 ≤ ratio | **はっきりと** | しきい値の2倍以上 |

度合い語は **主所見（最大 ratio の1件）にのみ** 付ける。副所見には付けない（文が重くなるため）。

---

## 4. 所見の描写テーブル（key × 符号 → desc / impact）

`desc`＝特徴の描写（「〜な姿勢」「〜なクセ」で終わる名詞句、先頭に度合い語が付く）。
`impact`＝その特徴が日常でどう出やすいか（1文）。**すべて非医療的な言い回し**。

### 側面

| key・符号 | desc（度合い語の後ろ） | impact |
|---|---|---|
| `forward_head` ＋ | 頭が肩より前に出やすい姿勢 | スマホやパソコンを見る時間が長い方に多く、首すじや肩の上のあたりが疲れやすくなります。 |
| `forward_head` − | 頭が後ろぎみに引けた姿勢 | 首の後ろがつまりやすく、上を向く動きがしにくく感じることがあります。 |
| `shoulder_forward` ＋ | 肩が内側に巻き込まれやすい姿勢（いわゆる巻き肩） | 胸の前が縮こまって背中側が伸ばされ、肩まわりが動かしにくく感じやすくなります。 |
| `shoulder_forward` − | 肩が後ろぎみに引けた姿勢 | 胸を張った状態が続きやすく、肩や背中の上のあたりが張りやすくなります。 |
| `trunk_lean` ＋（前傾） | 上半身が前に傾きやすい姿勢 | 腰や背中で体を支える時間が長くなり、腰まわりが張りやすくなります。 |
| `trunk_lean` −（後傾） | 上半身が後ろに傾きやすい姿勢 | 背中を丸めてバランスを取りやすく、背中や腰の張りにつながることがあります。 |
| `knee` 過伸展 | 立つときに膝が後ろへ反りやすいクセ | 膝の裏やふくらはぎに力が入り続けやすく、脚が疲れやすくなります。 |
| `knee` 屈曲位 | 膝を軽く曲げたまま立つクセ | 太ももの前や膝まわりがつねに働きやすく、脚が疲れやすくなります。 |

### 正面（左右は計測符号から自動決定）

| key・符号 | desc（度合い語の後ろ） | impact |
|---|---|---|
| `shoulder_tilt` | 左右の肩の高さに差が出やすい姿勢（**{低い側}肩が下がりぎみ**） | 左右どちらかの肩や首まわりに力が偏りやすく、肩の張りに左右差が出やすくなります。 |
| `pelvic_tilt` | 骨盤の高さに左右差が出やすい姿勢（**{低い側}側が下がりぎみ**） | 腰や股関節まわりの働きに左右差が出て、腰の片側が張りやすくなります。 |
| `head_tilt` | 頭が **{傾く側}** 側にかたむきやすい姿勢 | 首の{傾く側}側が緊張しやすく、首すじの張りにつながることがあります。 |
| `lateral_shift` | 上半身の中心が **{ずれる側}** 側へずれやすい姿勢 | 体の左右で支える力に差が出て、腰や骨盤まわりのバランスがかたよりやすくなります。 |

**左右の決定**：
- `shoulder_tilt` 低い側 ＝ 値>0 なら「右」、値<0 なら「左」
- `pelvic_tilt` 低い側 ＝ 値>0 なら「右」、値<0 なら「左」
- `head_tilt` 傾く側 ＝ 値>0 なら「右」、値<0 なら「左」
- `lateral_shift` ずれる側 ＝ 値>0 なら「右」、値<0 なら「左」

---

## 5. 所見の優先順位

複数の所見が採用された場合、**ratio 降順** に並べる。
ratio が同点のときは以下の固定順位（priority 昇順）で安定させる：

1 forward_head → 2 shoulder_forward → 3 trunk_lean → 4 knee →
5 shoulder_tilt → 6 pelvic_tilt → 7 head_tilt → 8 lateral_shift

- **主所見** ＝ 並べた先頭（1件）。度合い語つきで描写し、その impact を第2文に使う。
- **副所見** ＝ 2番目（存在すれば）。度合い語なしで desc のみ第1文に併記する。
- 3件目以降は本文に出さない（読みやすさ優先。筋肉・種目リストには反映される）。

---

## 6. 申告部位の一文（`painAreas` → 1文）

`painAreas` の配列を先頭から見て、最初に対応表に載っている部位の一文を **1つだけ** 採用する。

| ID | 一文 |
|---|---|
| `neck` | 気になっている首まわりのハリとも、関わりやすい姿勢です。 |
| `shoulder` | 気になっている肩まわりのハリとも、つながりやすい姿勢です。 |
| `midback` | 気になっている背中のハリとも、関わりやすい姿勢です。 |
| `lowback` | 気になっている腰まわりのハリとも、関わりやすい姿勢です。 |
| `hip` | 気になっている股関節まわりの感覚とも、関わりやすい姿勢です。 |
| `knee` | 気になっている膝まわりの感覚とも、関わりやすい姿勢です。 |
| `calf` | 気になっているふくらはぎのハリとも、関わりやすい姿勢です。 |
| `thigh_front` | 気になっている太もも前のハリとも、関わりやすい姿勢です。 |
| `thigh_back` | 気になっている太もも裏のハリとも、関わりやすい姿勢です。 |

医療的な断定（「〜が原因です」等）は避け、「〜とも関わりやすい姿勢です」と柔らかく接続する。

---

## 7. 見通しの一文（週頻度 → 最終文）

週頻度ラベル：`freq>=5 → 週5回以上`、それ以外は `週{freq}回`。

- 所見あり：`{週ラベル}のトレーニングと、こわばった筋肉のストレッチを続けることで、少しずつ整えていけます。`
- 所見なし（理想）：`{週ラベル}のトレーニングとストレッチで、今の良い状態をキープしていきましょう。`

---

## 8. 文の組み立て（最終アセンブリ）

### 所見が1件以上あるとき

```
[第1文]
  ・主所見のみ            → 「{度合い語}{主desc}です。」
  ・主所見＋副所見あり     → 「{度合い語}{主desc}に加えて、{副desc}も見られます。」
[第2文]  主所見の impact
[第3文]  申告部位の一文（painAreas があるときのみ）
[最終文] 見通しの一文（週頻度反映）
```
→ 申告なしで3文、申告ありで4文。

### 所見が1件も無いとき（理想姿勢）

```
[第1文]  大きな姿勢の崩れは見られず、全体にバランスの取れた状態です。
[第2文]  申告部位の一文（painAreas があるときのみ）
[最終文] 見通しの一文（理想版・週頻度反映）
```
→ 申告なしで2文、申告ありで3文。

> 側面・正面の両方が未検出などで計測値自体が無い場合も、この理想姿勢文に落ちる。

---

## 9. 出力例（実装での実測）

| 入力 | 出力 |
|---|---|
| FHP中等度＋巻き肩・週2・肩こり | やや頭が肩より前に出やすい姿勢に加えて、肩が内側に巻き込まれやすい姿勢（いわゆる巻き肩）も見られます。スマホやパソコンを見る時間が長い方に多く、首すじや肩の上のあたりが疲れやすくなります。気になっている肩まわりのハリとも、つながりやすい姿勢です。週2回のトレーニングと、こわばった筋肉のストレッチを続けることで、少しずつ整えていけます。 |
| 肩右下がり＋骨盤左右差・週3・腰痛 | やや左右の肩の高さに差が出やすい姿勢（右肩が下がりぎみ）に加えて、骨盤の高さに左右差が出やすい姿勢（右側が下がりぎみ）も見られます。左右どちらかの肩や首まわりに力が偏りやすく、肩の張りに左右差が出やすくなります。気になっている腰まわりのハリとも、関わりやすい姿勢です。週3回のトレーニングと、こわばった筋肉のストレッチを続けることで、少しずつ整えていけます。 |
| 逸脱なし・週3・申告なし | 大きな姿勢の崩れは見られず、全体にバランスの取れた状態です。週3回のトレーニングとストレッチで、今の良い状態をキープしていきましょう。 |

---

## 10. 変更したいときの入口

| 変えたいこと | 触る場所 |
|---|---|
| 所見を拾う感度（過検出・過小検出） | `src/pose/thresholds.js` の `WARN` / `ENTRY_RATIO` |
| 度合い語の区切り（わずかに／やや／はっきりと） | `diagnosis.js` `severityWord()` |
| 特徴の言い回し・影響文 | `diagnosis.js` `collectFindings()` の各 `desc` / `impact` |
| 申告部位の一文 | `diagnosis.js` `PAIN_SENTENCE` |
| 見通しの言い回し | `diagnosis.js` `outlookSentence()` と理想姿勢分岐 |
| 文の組み立て順・文数 | `diagnosis.js` `buildDiagnosis()` |

いずれも編集後は `./scripts/bump-cache.sh` を実行してから deploy すること。

---

## 11. 注意

- 本文は医学的診断ではなく、姿勢の見た目の傾向を平易に伝えるもの。
- 計測は MediaPipe Pose の推定値であり誤差を含む。撮影条件（距離・カメラ高さ・服装）で変動する。
- 施術判断は有資格者が別途行う前提（レポートのフッター文言で明示）。
