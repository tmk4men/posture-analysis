# UGOQ仕様 実装ルールブック

出典：**IMG_2008.jpg「姿勢分析AIシステム設計（UGOQ仕様）」**

- 実装：`src/pose/postureTypes.js`
- 突き合わせテスト：`検証/ugoq-spec.test.mjs`（`node --test 検証/ugoq-spec.test.mjs`）
- しきい値：`src/pose/thresholds.js`（`DIAGNOSIS_RULEBOOK.md` と共有）

**最終更新**：2026-07-27

---

## 1. 仕様書のどこを実装したか

| 仕様書 | 実装 |
|---|---|
| ① 姿勢分析マスター | `UPPER_TYPES` / `LOWER_TYPES`（姿勢名・判定項目・主な改善ポイントを原文どおり保持） |
| ② ラクレッチマスター | `EXERCISE_ASSETS` の 5 種（CHEST / SHOULDER / TWISTER / HIP / ADDUCTOR） |
| ③ 筋力トレーニングマスター | `EXERCISE_ASSETS` の 12 種 |
| ④ メニュー処方マスター | `PRESCRIPTION`（7行を種目・順番ごと保持） |
| ⑤ メニュー生成ロジック STEP1〜8 | STEP1＝写真アップロード、STEP2〜8＝`analyzePosture()` |
| ⑥ ユーザー画面イメージ | レポート1ページ目の「姿勢分析結果」＋2ページ目の「本日のメニュー」 |

---

## 2. STEP2／STEP3 — 姿勢タイプの判定

計測値（`src/pose/angles.js`）を臨床しきい値で割った **ratio** を求め、
**1.0 以上のうち最大の候補**を採用する。1.0 を超えるものが無ければ正常（U1／L1）。

### 上半身（側面写真）

| タイプ | 条件 | ratio |
|---|---|---|
| U2 猫背・巻き肩 | `shoulder_forward > 0` | `shoulder_forward ÷ 8` |
| U3 前方頭位 | `forward_head > 0` | `forward_head ÷ 10` |
| U4 フラットバック | `shoulder_forward < 0` かつ `forward_head < 10` | `\|shoulder_forward\| ÷ 8` |
| U1 正常 | 上記がどれも 1.0 未満 | — |

### 下半身（側面＋正面写真）

| タイプ | 条件 | ratio |
|---|---|---|
| L2 骨盤前傾（反り腰） | `trunk_lean > 0`（体幹前傾） | `trunk_lean ÷ 5` |
| L3 スウェイバック | `pelvis_shift > 0` かつ `shoulder_forward < 0` | `pelvis_shift ÷ 8` |
| L4 O脚 | `knee_alignment > 0` | `knee_alignment ÷ 4` |
| L4 X脚 | `knee_alignment < 0` | `\|knee_alignment\| ÷ 4` |
| L1 正常 | 上記がどれも 1.0 未満 | — |

### 直接は測れない判定項目（重要）

仕様書の判定項目のうち、骨格33点からは測れないものは **代理指標** を使っている。
実測データが増えたら見直す前提の箇所なので、ここを暗黙の前提にしないこと。

| 仕様書の判定項目 | 直接測れない理由 | 代理指標 |
|---|---|---|
| U4「胸椎カーブ減少・胸椎可動性低下」 | 胸椎の弯曲は肩と骨盤の2点からは出せない | 胸郭が骨盤より後方（`shoulder_forward < 0`）かつ頭部前方位が小さい |
| L2「骨盤前傾・腰椎前弯増加」 | MediaPipe は骨盤を左右1点ずつしか返さず ASIS/PSIS が取れない | 体幹前傾（`trunk_lean > 0`） |
| L4「膝アライメント異常」 | — | 股関節→足首の線から膝が外れた量（下肢長比）。左右の膝間隔ではなく、この方法なら立ち幅に左右されない |

---

## 3. STEP4 — 上半身・下半身の組み合わせ

上下それぞれに3種目の処方があるので、合計6種目からラクレッチ3・筋トレ3に絞る。
方針は **逸脱の大きい側を優先**。

| 状態 | 割り振り | `menu.balance` |
|---|---|---|
| 上半身の ratio が大きい | 上半身①② ＋ 下半身① | `upper_led` |
| 下半身の ratio が大きい | 下半身①② ＋ 上半身① | `lower_led` |
| 上半身のみ崩れ | 上半身①②③ | `upper_only` |
| 下半身のみ崩れ | 下半身①②③ | `lower_only` |
| 上下とも正常 | 維持メニュー | `maintenance` |

重複した種目は、優先側の次の候補で埋める（必ず3種目・重複なしになる）。

### 副側が下半身のときは下肢の種目を先に採る

⑥ ユーザー画面イメージの例（U2猫背・巻き肩 × L2反り腰）は
筋トレが **ラットプルダウン／ローイング／ヒップスラスト**。
④ の骨盤前傾の行は ①アブドミナル ②ヒップスラスト なので、
単純に①から採ると⑥と食い違う。

そこで **下半身の処方から1種目だけ採るときは下肢の種目を先に見る**
（`orderForSecondary()`）。④ の表そのものは一切書き換えていない。
アブドミナルは体幹の種目で、上半身側から採った2種目と役割が重なりやすいという意味でも筋が通る。
実際に順番が変わるのは **L2 を副側から1種目だけ採るとき** に限られ、
L3・L4 は元から①が下肢種目なので影響しない。

この規則により ⑥ の例はそのまま再現される（`検証/ugoq-spec.test.mjs` で固定）。

> **正常（U1×L1）の維持メニュー**：④ に「正常」の行が無いためこちらで起こし、
> **2026-08-02 に先方がこの内容で採用と回答**した。
> ラクレッチ CHEST／HIP／TWISTER ＋ 筋トレ ローイング／アブドミナル／ヒップスラスト。
> 目的は「姿勢改善」ではなく「**姿勢維持**」なので、レポート2ページ目のリード文も
> `balance === "maintenance"` のときだけ維持向けの文面に切り替えている（`src/ui/report.js`）。

---

## 4. 種目名 → アセットの対応

| 仕様書の種目名 | アセットid | 備考 |
|---|---|---|
| CHEST / SHOULDER / TWISTER / HIP / ADDUCTOR | `stretch_chest` / `stretch_shoulder` / `stretch_twister` / `stretch_hip` / `stretch_adductor` | |
| ラットプルダウン | `lat_pulldown` | |
| ローイング | `seated_row` | シーテッドロウ |
| リアデルト | `pec_fly_rear_delt` | ペックフライ、リアデルト |
| バックエクステンション | `back_extension_bench` | |
| ヒップスラスト | `hip_thrust` | |
| アブドミナル | `abdominal` | |
| レッグプレス | `seated_leg_press` | パワーレッグプレスもあるがシーテッドを採用 |
| レッグカール | `seated_leg_curl` | |
| レッグエクステンション | `leg_extension` | X脚の行だけ「VMOを意識」の注記つき |
| ヒップアブダクション | `hip_abduction` | 「インナー、アウターサイ」の外転モード。画像は共用 |
| アダクター | `adductor_machine` | 同・内転モード。画像は共用 |
| カーフレイズ | `calf_raise` | **専用マシンは無い**。レクスコの**レッグプレス／シーテッドレッグプレス**で「脚を伸ばした状態でつま先立ち」をして行う（先方確定・2026-08-02）。カードはシーテッドレッグプレスのカードから起こした（`scripts/build-machine-cards.py`） |

### カード画像の作り直し（2026-08-02）

先方支給のカード37枚のうち、**ラクレッチ5種は新型マシン（Rakuretch ロゴ入り）の写真**に差し替えた。
届いたのはマシン単体写真だったので、`scripts/build-machine-cards.py` が
既存カードの白い写真枠を検出し、**写真部分だけ**を入れ替えている（タイトル・キャッチ・
運動のポイント・下部ラベルは支給カードのまま）。カーフレイズのカードも同じスクリプトで作る。

- 入力（`.gitignore` 済みの先方資料）：`素材/ラクレッチ改/*.jpg`、`レクスコマシン/`
- 出力：`assets/exercises/rakuretch-*.webp`、`assets/exercises/calf-raise.webp`
- ファイル名を UUID から変えたのは、同名で上書きすると GitHub Pages のブラウザキャッシュで
  しばらく旧写真が出るため。旧5枚は再生成の土台に使うので消していない。

---

## 5. ② と ④ の関係（先方確定・2026-08-02）

**アプリの正式マスターデータは ④ メニュー処方マスター。** ② と ④ は一致しなくてよい。
先方の管理上も ② のタイトルは「**主な適応姿勢**」、④ は「**処方メニュー**」に改称される。

| 箇所 | ②「主な適応姿勢」 | ④ 処方メニューでの使われ方と目的 |
|---|---|---|
| HIP | L2・L3・L4 | **フラットバック**の②にも入る。骨盤可動性・股関節伸展・骨盤前後傾の誘導が目的 |
| TWISTER | U2・U3・U4・L3 | **骨盤前傾・O脚・X脚**の③にも入る。胸椎回旋・骨盤回旋・体幹連動の改善が目的 |

つまり ② に載っていない組み合わせは「直接適応」ではなく「**姿勢改善を補助する役割**」。
仕様書の見た目上の食い違いは意図されたもので、実装は ④ のまま。差し替え不要。

---

## 6. 変更したいときの入口

| 変えたいこと | 触る場所 |
|---|---|
| 姿勢タイプの判定基準（感度） | `src/pose/thresholds.js` の `WARN`（`pelvis_shift` / `knee_alignment` を含む） |
| 判定に使う条件そのもの | `postureTypes.js` の `classifyUpper()` / `classifyLower()` |
| 処方する種目 | `postureTypes.js` の `PRESCRIPTION` |
| 上下の組み合わせ方 | `postureTypes.js` の `buildMenu()` |
| ウォーミングアップ・有酸素・クールダウンの文言 | `postureTypes.js` の `WARMUP` / `CARDIO` / `COOLDOWN` |
| レポートの見た目 | `src/ui/report.js` の `postureResultHtml()` と page2、`app.css` |

いずれも編集後は `node --test 検証/` を通し、deploy 前に `./scripts/bump-cache.sh` を実行すること。
